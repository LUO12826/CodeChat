<Page
    x:Class="CAC.client.testPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CAC.client"
    xmlns:cc="using:CAC.client.CustomControls"
    xmlns:global="using:CAC.client.Global"
    xmlns:mg="using:CAC.client.MessagePage" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>


    </Page.Resources>
    
    <Grid>
        <!--测试表情时的代码-->
        <!--<GridView ItemsSource="{x:Bind group.Items}" Width="300">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="global:Emoji">
                    <TextBlock x:Phase="0" Text="{x:Bind EmojiString}"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>-->
        <cc:InputBox Margin="0,170,0,0"/>

        <!--<mg:MessageViewer/>-->
    </Grid>
</Page>
